import React, { Component } from "react";

export default class History extends Component {
  render() {
    return (
      <div>
        <h2>This is where the history chart will be!</h2>
      </div>
    );
  }
}
